#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
  int N,M;
  cin >> N >> M;
  vector<int> target;
  for (int i = 0; i < M; ++i) {
    int tmp;
    cin >> tmp;
    target.push_back(tmp);
  }
  sort(target.begin(), target.end());

  vector<int> piece_location;

  int result = 100010;

  // 左から敷く
  while()


  // 右から敷く

  return 0;
}